# Week 5

This week's work includes three modules:

## Module 1
- Practiced creating a GitHub branch and uploading .cpp and .h files.

## Module 6 — Strings and Classes
- Created an `Item` class with name and quantity.
- Implemented `saveToFile()` to save item data into items.txt.
- Implemented `loadFromFile()` to read and display the saved data.

## Module 11 — Constructors & Destructors
- Created a `Product` class with id, name, and price.
- Implemented a constructor and destructor to show object lifecycle.
- Used `printDetails()` to display product information.
